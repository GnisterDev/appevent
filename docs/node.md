# Grunnleggende om NodeJS

NodeJS er en **runtime environment** som lar deg kjøre `JavaScript` utenfor nettleseren.

## npm

**npm** er en **package manager** for `JavaScript`, på samme måte som hva `pip` er for `Python` eller `Maven` er for `Java`.

**[Tilbake til oversikt](./README.md)**
